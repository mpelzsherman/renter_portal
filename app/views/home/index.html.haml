%ul.nav
  - if !current_tenant
    %li= link_to 'Sign In as Tenant', new_tenant_session_path
  - else
    %li= link_to "Sign Out from Tenant #{current_tenant.email}", destroy_tenant_session_path, method: 'delete'

  - if !current_landlord
    %li= link_to 'Sign In as Landlord', new_landlord_session_path
  - else
    %li= link_to "Sign Out from Landlord #{current_landlord.email}", destroy_landlord_session_path, method: 'delete'


  %li=link_to "View Payments", controller: :payments
